# Production secrets
.env.production

# Local env files (never commit real secrets)
.env
.env.local

# Deploy history
.deploy-history

# TLS certificates
nginx/certs/*.pem

# Local development secrets
.env.development
UI
/UI/
./UI/*